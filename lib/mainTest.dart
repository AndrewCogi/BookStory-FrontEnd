import 'package:amplify_core/amplify_core.dart';

void main(){
  // double x = MediaQuery.of(context).size.width;
  // double m = -0.13 / 489;
  // double b = 0.26 + (0.13 / 489) * 411;
  // double labelSizeFactor = m * x + b;
  // safePrint(labelSizeFactor);
}
